const points = [10, 8, 4, 6, 5];
// This delete the last element from the list
points.pop();

console.log(points);


let average = (points[0], points[1], points[2], points[3]) / points.length;

console.log(average);